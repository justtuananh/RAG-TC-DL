# Spike A — structural + formula extraction (lightweight, offline)
olefile>=0.47      # read MathType "Equation Native" (MTEF) streams from OLE objects
lxml>=5.0          # parse word/document.xml in document order
scikit-image>=0.21 # QA gate SSIM (image similarity scoring for formula render comparison)
python-dotenv>=1.0 # load .env for local dev runs (generation.py: OLLAMA_MODEL / OLLAMA_URL)

# Task 3 — retrieval + UI (run in kotaemon/.venv)
# qdrant-client==1.10.1  (already in kotaemon/.venv — matches Qdrant server v1.10.1)
# gradio>=4.39           (already in kotaemon/.venv)
rank-bm25>=0.2.2   # BM25 in-memory index for hybrid retrieval (MIT)
# requests, rank_bm25, gradio installed via: cd kotaemon && uv pip install rank-bm25

# PDF ingestion (Stage 1) — extract_pdf.py: text structure only, no
# MathType/OLE formula recovery (PDFs carry no such object to parse).
# PyMuPDF, not MarkItDown/pdfminer — the latter mangled text into
# space-less runs on some real-world PDFs (broken embedded font width table).
pymupdf>=1.24

# Sprint 0+1 — Database and Authentication
sqlalchemy>=2.0       # ORM for PostgreSQL
psycopg2-binary>=2.9  # PostgreSQL adapter for Python
bcrypt>=4.0           # Password hashing
pyjwt>=2.8            # JWT token generation and validation
alembic>=1.13         # Database migrations
fastapi>=0.104        # Web framework (for api_server.py)
python-multipart>=0.0.6  # Form parsing for FastAPI